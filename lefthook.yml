pre-commit:
  commands:
    prettier:
      run: npx prettier . --write --ignore-path .prettierignore
      stage_fixed: true
    lint:
      run: npx eslint . --fix --ignore-path .eslintignore
      stage_fixed: true

import React, { useEffect } from "react";

const Agrobot = () => {

  useEffect(() => {
    window.scrollTo(0, 0);
  }, []);

  return (
    <main
      style={{
        overflow: 'hidden',
      }}
    >
    </main >
  );
};

export default Agrobot;
